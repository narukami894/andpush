language: ruby
cache:    bundler
script:   bundle exec rake test
sudo:     false

before_install:
  - gem update --system
  - gem install bundler

rvm:
  - 2.2.10
  - 2.3.8
  - 2.4.9
  - 2.5.7
  - 2.6.5
  - ruby-head
  - jruby-9.2.9.0
  - jruby-head

matrix:
  allow_failures:
    - rvm: ruby-head
    - rvm: jruby-9.2.5.0
    - rvm: jruby-head
